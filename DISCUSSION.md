Further improvements:

First off, I would have added testing to this - both unit and integration at the very list, and tested this more thoroughly.

Second off, I would have likely added infinite scrolling to support loading more records than a single page would hold (a paginated view seemed more quick, but I would have liked to implement that).

I also would have added a sorting feature, as well as additional filtering options to the search (i.e. a location based filter, a credential based filter, a sepecialty based filter, etc.)